FROM node:20-alpine
WORKDIR /app
RUN addgroup -S vault && adduser -S vault -G vault
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN chown -R vault:vault /app
USER vault
CMD ["node", "upload/queues/scanWorker.js"]
